on:
  twitter:
    screen_name: test
jobs:
  print:
    name: Print
    runs-on: ubuntu-latest
    steps:
      - name: Print Outputs
        env:
          text: ${{ on.twitter.outputs.text }}
        run: |
          echo text: $text
